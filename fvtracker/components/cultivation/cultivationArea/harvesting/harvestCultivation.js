export const HarvestCultivation = ({
  isOpen,
  onCancel,
  onSubmit,
  newNewHarvest,
  setNewHarvest,
  availablePlans,
  crops,
  onChoosePlan,
  submitDisabled,
}) => {
  return (
    <Modal isOpen={isOpen} onClose={onCancel}>
      berba brate
    </Modal>
  );
};
