import { fieldsList, fetchFieldBySlug } from "./fields";

export default {
    fields: {
        fieldsList,
        fetchFieldBySlug
    },
    cultivationManager: {
        fetchCultivationManager
    }
}