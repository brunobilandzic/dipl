export const getPlantedCropVarietesPerCultivation = ({ items }) => {
  const cultivationPlantedCropVarieties = {};
  items.forEach((item) => {
    const plantedCropVarieties = item.plantedCropVarieties || [];
  });
};
